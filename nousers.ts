

let users = ["Omer","Saad","Ali"];
if(users.length === 0) {
    console.log(`We need to find some users!`)
} else {
    for(let a = 0; a<users.length; a++) {
        console.log(`Hello ${users[a]}!`)
    }
}
users = []

if(users.length === 0) {
    console.log(`We need to find some users!`)
}